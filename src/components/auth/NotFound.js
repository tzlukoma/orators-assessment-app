import React from 'react'

const NotFound = () => {
    return (
        <div className="pink-text container center" style={{ margin: 'auto', padding:20 }}>
            <h4>Los siento!! You have reached a page that does not exist</h4>
        </div>
    )
}

export default (NotFound)