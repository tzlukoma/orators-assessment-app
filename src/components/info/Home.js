import React from 'react';

const Home = () => {
    return (
        <div>
            This is the home page where we describe everything about the app.
        </div>
    );
};

export default Home;